# Green Goblin model

`green_goblin_2002.glb` is an original team-provided event asset. It is kept in
the repository so local builds and shared copies use the same boss model.

The source model includes separate Green Goblin and glider rigs but no embedded
animation clips. Runtime movement and hit reactions are implemented in Godot.
